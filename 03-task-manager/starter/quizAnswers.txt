1. middleware is a function that runs to perform a particular logic before a response is sent back to the user.

2. C ---> Create... R ---> Read... U ---> Update... D ---> Delete.

3.1 the app is able to access the information in the .env file using the Process.env.

3.2 by creating a .gitignore and adding the .env in it so that it would not be sent down to github.


